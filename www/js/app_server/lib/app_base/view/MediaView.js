// Generated by CoffeeScript 1.3.3
(function() {

  define(function(require, exports, module) {
    var EaselView, EdgeView, MediaController, MediaView, backbone, self;
    self = void 0;
    backbone = require("backbone");
    MediaController = require("app_base/controller/MediaController");
    EdgeView = require("app_base/view/EdgeView");
    EaselView = require("app_base/view/EaselView");
    MediaView = Backbone.View.extend({
      el: "body",
      className: "media-view",
      events: {},
      initialize: function(options) {
        self = this;
        return this.onResize();
      },
      loadMedia: function(options) {
        if (options.type.match('edge')) {
          this.loadEdge(options);
        }
        if (options.type.match('easel')) {
          return this.loadEasel(options);
        }
      },
      loadEdge: function(options) {
        var uid;
        uid = 'media_' + String(Math.random()).substring(6);
        return this[uid] = new EdgeView(options);
      },
      loadEasel: function(options) {
        var uid;
        uid = 'media_' + String(Math.random()).substring(6);
        return this[uid] = new EaselView(options);
      },
      onResize: function(e) {
        self.sw = parseInt($(window).width());
        return self.sh = parseInt($(window).height());
      },
      destroy: function() {}
    });
    return module.exports = MediaView;
  });

}).call(this);
